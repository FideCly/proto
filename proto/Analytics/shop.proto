syntax = "proto3";

package analytics;

service ShopService {
  rpc Send (SendRequest) returns (SendResponse) {}
}

message SendRequest {
    int32 id = 1;
    bool isActive = 6;
}

message SendResponse {
  int32 status = 1;
  repeated string errors = 2;
}
