syntax = "proto3";

package analytics;

service CardService {
  rpc Send (SendRequest) returns (SendResponse) {}
}

message SendRequest {
    int32 id = 1;
    int32 shopId = 2;
    int32 userId = 2;
    string startAt = 3;
    string endAt = 4;
    bool isActive = 5;
}

message SendResponse {
  int32 status = 1;
  repeated string errors = 2;
}
