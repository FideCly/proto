syntax = "proto3";

package analytics;

service ShopService {
  rpc Send (SendRequest) returns (SendResponse) {}
}

message SendRequest {
  int32 id = 1;
  bool is_active = 6;
}

message SendResponse {
  int32 status = 1;
  repeated string errors = 2;
}
